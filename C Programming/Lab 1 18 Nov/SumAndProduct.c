#include <stdio.h>
#include <stdlib.h>

int main()
{
   int a,b;
   printf("Enter two numbers: ");
   scanf("%d %d",&a,&b);
   printf("The sum of the two numbers is %d and their product is %d",(a+b),(a*b));
   return 0;
}

